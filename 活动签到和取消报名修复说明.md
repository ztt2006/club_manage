# 活动签到和取消报名功能修复说明

## 🐛 问题描述

**现象**：点击签到和取消报名按钮时，显示"请求的资源不存在"

**原因**：
- 前端API调用：`/api/activity-records/:recordId/checkin` 和 `/api/activity-records/:recordId`
- 后端没有注册 `activity-records` 路由
- 导致404错误

---

## ✅ 修复方案

### 1. 创建活动记录路由
**文件**：`back/routes/activity-record.routes.js`

```javascript
const express = require('express');
const router = express.Router();
const activityRecordController = require('../controllers/activity-record.controller');
const { auth, checkRole } = require('../middleware/auth.middleware');

// 签到（管理员）
router.put('/:id/checkin', auth, checkRole('ADMIN', 'SUPER_ADMIN'), activityRecordController.checkIn);

// 删除报名记录（管理员）
router.delete('/:id', auth, checkRole('ADMIN', 'SUPER_ADMIN'), activityRecordController.deleteRecord);

module.exports = router;
```

**权限**：仅管理员（ADMIN、SUPER_ADMIN）可操作

---

### 2. 创建活动记录控制器
**文件**：`back/controllers/activity-record.controller.js`

**主要功能**：

#### checkIn（签到）
- 查找报名记录
- 更新状态为 `CHECKED_IN`
- 记录签到时间
- 发送通知给用户

#### deleteRecord（取消报名）
- 查找报名记录
- 删除记录
- 发送通知给用户

---

### 3. 注册路由
**文件**：`back/server.js`

```javascript
app.use('/api/activity-records', require('./routes/activity-record.routes'));
```

---

### 4. 添加取消活动功能
**文件**：`back/controllers/activity.controller.js`

**新增方法**：`cancelActivity`
- 获取当前活动状态
- 切换状态（CANCELLED ↔ PLANNED）
- 返回更新结果

**路由**：`PUT /api/activities/:id/cancel`

---

## 📋 完整API列表

### 活动记录相关
- `PUT /api/activity-records/:recordId/checkin` - 签到（管理员）
- `DELETE /api/activity-records/:recordId` - 取消报名（管理员）

### 活动管理相关
- `PUT /api/activities/:id/cancel` - 取消/恢复活动（管理员）

---

## 🔄 功能流程

### 签到流程
```
管理员点击"签到" 
  ↓
前端：PUT /api/activity-records/123/checkin
  ↓
后端：查找记录
  ↓
更新状态：REGISTERED → CHECKED_IN
  ↓
记录签到时间
  ↓
发送通知：签到成功
  ↓
返回：✅ 签到成功
```

### 取消报名流程
```
管理员点击"取消报名"
  ↓
前端：DELETE /api/activity-records/123
  ↓
后端：查找记录
  ↓
删除报名记录
  ↓
发送通知：报名已取消
  ↓
返回：✅ 取消成功
```

### 取消活动流程
```
管理员点击"取消活动"
  ↓
前端：PUT /api/activities/456/cancel
  ↓
后端：查找活动
  ↓
切换状态：PLANNED → CANCELLED
  ↓
返回：✅ 活动已取消
```

---

## 🎯 权限控制

### 签到和取消报名
```
权限：ADMIN、SUPER_ADMIN
限制：普通用户不能操作
```

### 通知发送
```
签到成功 → 通知用户
报名取消 → 通知用户
```

---

## 🚀 部署步骤

### 1. 重启后端服务
新增了路由和控制器，需要重启服务：

```bash
cd back
# 如果服务正在运行，按 Ctrl+C 停止
npm run dev
```

### 2. 验证修复
使用管理员账号测试：

```
1. 登录：admin / 123456
2. 进入：活动详情页
3. 查看：报名成员列表
4. 操作：
   - 点击"签到" → ✅ 成功
   - 点击"取消报名" → ✅ 成功
5. 点击：取消活动 → ✅ 成功
```

---

## ✅ 修复完成清单

- [x] 创建活动记录路由
- [x] 创建活动记录控制器
- [x] 注册路由到server.js
- [x] 添加签到功能
- [x] 添加取消报名功能
- [x] 添加取消活动功能
- [x] 权限控制（仅管理员）
- [x] 通知发送功能

---

## 📝 测试用例

### 测试1：签到功能
```
前提：有用户报名了活动
步骤：
1. 管理员进入活动详情
2. 查看报名成员列表
3. 点击某个成员的"签到"按钮
预期：
- ✅ 状态变为"已签到"
- ✅ 显示签到时间
- ✅ 用户收到通知
```

### 测试2：取消报名
```
前提：有用户报名了活动
步骤：
1. 管理员进入活动详情
2. 查看报名成员列表
3. 点击某个成员的"取消报名"按钮
4. 确认操作
预期：
- ✅ 成员从列表中移除
- ✅ 报名人数减少
- ✅ 用户收到通知
```

### 测试3：取消活动
```
前提：有一个计划中的活动
步骤：
1. 管理员进入活动详情
2. 点击"取消活动"按钮
3. 确认操作
预期：
- ✅ 活动状态变为"已取消"
- ✅ 按钮文字变为"恢复活动"
- ✅ 用户无法再报名
```

---

## 🔧 相关文件

**新增文件**：
- ✅ `back/routes/activity-record.routes.js`
- ✅ `back/controllers/activity-record.controller.js`

**修改文件**：
- ✅ `back/server.js`（注册路由）
- ✅ `back/routes/activity.routes.js`（添加取消活动路由）
- ✅ `back/controllers/activity.controller.js`（添加cancelActivity方法）

**前端文件**（无需修改）：
- ✅ `front/src/api/activity.js`
- ✅ `front/src/views/ActivityDetail.vue`

---

**修复时间**：2024  
**版本**：v2.0.2  
**状态**：✅ 已修复，需要重启后端服务
