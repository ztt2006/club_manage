# 权限问题修复说明

## 🐛 问题描述

### 问题1：招新功能权限不足
**现象**：使用超级管理员和管理员账号发布招新时，显示权限不足

**原因**：招新路由使用了 `checkClubRole` 中间件，要求用户必须是特定社团的社长或副社长才能创建招新。但创建招新时还没有选择社团，导致权限检查失败。

### 问题2：用户加入社团权限不足
**现象**：普通用户申请加入社团时显示权限不足

**原因**：成员管理路由的 `addMember` 使用了 `checkClubRole` 中间件，要求必须是社团管理层才能添加成员。但普通用户加入社团时还不是成员，导致权限检查失败。

---

## ✅ 修复方案

### 修复1：招新功能权限

**文件**：`back/routes/recruitment.routes.js`

**修改前**：
```javascript
const { auth, checkClubRole } = require('../middleware/auth.middleware');

router.post('/', auth, checkClubRole('PRESIDENT', 'VICE_PRESIDENT'), recruitmentController.createRecruitment);
router.put('/:id/status', auth, checkClubRole('PRESIDENT', 'VICE_PRESIDENT'), recruitmentController.updateRecruitmentStatus);
```

**修改后**：
```javascript
const { auth, checkRole } = require('../middleware/auth.middleware');

router.post('/', auth, checkRole('ADMIN', 'SUPER_ADMIN'), recruitmentController.createRecruitment);
router.put('/:id', auth, checkRole('ADMIN', 'SUPER_ADMIN'), recruitmentController.updateRecruitment);
router.delete('/:id', auth, checkRole('ADMIN', 'SUPER_ADMIN'), recruitmentController.deleteRecruitment);
router.put('/:id/status', auth, checkRole('ADMIN', 'SUPER_ADMIN'), recruitmentController.updateRecruitmentStatus);
```

**效果**：
- ✅ 管理员（ADMIN）可以创建和管理招新
- ✅ 超级管理员（SUPER_ADMIN）可以创建和管理招新
- ❌ 普通用户（USER）不能创建招新

---

### 修复2：用户加入社团权限

#### 2.1 路由层修改

**文件**：`back/routes/member.routes.js`

**修改前**：
```javascript
router.post('/', auth, checkClubRole('PRESIDENT', 'VICE_PRESIDENT', 'MANAGER'), memberController.addMember);
```

**修改后**：
```javascript
// 普通用户可以自己加入社团，不需要社团角色权限
router.post('/', auth, memberController.addMember);
```

#### 2.2 控制器层安全检查

**文件**：`back/controllers/member.controller.js`

**添加的代码**：
```javascript
// 权限检查：普通用户只能添加自己
const isAdmin = ['ADMIN', 'SUPER_ADMIN'].includes(req.user?.role);
const isSelf = parseInt(userId) === req.user.id;

if (!isAdmin && !isSelf) {
  return res.status(403).json({
    success: false,
    message: '您只能添加自己加入社团'
  });
}

// 如果是普通用户添加自己，强制角色为 MEMBER
const memberRole = (isAdmin || !isSelf) ? (role || 'MEMBER') : 'MEMBER';
```

**效果**：
- ✅ 普通用户可以添加自己加入社团（角色自动为 MEMBER）
- ✅ 管理员可以添加任何人，并指定角色
- ❌ 普通用户不能添加其他人

---

## 🎯 权限矩阵

### 招新功能权限

| 操作 | SUPER_ADMIN | ADMIN | USER |
|------|-------------|-------|------|
| 创建招新 | ✅ | ✅ | ❌ |
| 编辑招新 | ✅ | ✅ | ❌ |
| 删除招新 | ✅ | ✅ | ❌ |
| 开启/关闭招新 | ✅ | ✅ | ❌ |
| 查看招新 | ✅ | ✅ | ✅ |
| 提交申请 | ✅ | ✅ | ✅ |
| 审核申请 | ✅ | ✅ | ❌ |

### 社团成员管理权限

| 操作 | SUPER_ADMIN | ADMIN | 社团管理层 | USER |
|------|-------------|-------|-----------|------|
| 添加自己 | ✅ | ✅ | ✅ | ✅ |
| 添加他人 | ✅ | ✅ | ✅ | ❌ |
| 删除成员 | ✅ | ✅ | ✅ | 自己可删除 |
| 修改角色 | ✅ | ✅ | 社长/副社长 | ❌ |
| 修改状态 | ✅ | ✅ | 社长/副社长 | ❌ |

### 社团管理权限

| 操作 | SUPER_ADMIN | ADMIN | USER |
|------|-------------|-------|------|
| 创建社团 | ✅ | ✅ | ❌ |
| 编辑社团 | ✅ | ✅ | ❌ |
| 删除社团 | ✅ | ✅ | ❌ |
| 查看社团 | ✅ | ✅ | ✅ |
| 加入社团 | ✅ | ✅ | ✅ |
| 退出社团 | ✅ | ✅ | ✅ |

---

## 📝 申请管理路由修复

**文件**：`back/routes/application.routes.js`

**修改后**：
```javascript
const { auth, checkRole } = require('../middleware/auth.middleware');

router.get('/recruitment/:recruitmentId', auth, checkRole('ADMIN', 'SUPER_ADMIN'), applicationController.getApplicationsByRecruitment);
router.get('/:id', applicationController.getApplicationById);
router.post('/', applicationController.createApplication);  // 所有人可提交申请
router.put('/:id/review', auth, checkRole('ADMIN', 'SUPER_ADMIN'), applicationController.reviewApplication);
router.delete('/:id', auth, checkRole('ADMIN', 'SUPER_ADMIN'), applicationController.deleteApplication);
```

---

## 🔐 安全说明

### 1. 分层权限检查
- **路由层**：基本的角色权限检查（ADMIN, SUPER_ADMIN等）
- **控制器层**：业务逻辑权限检查（是否本人操作等）

### 2. 最小权限原则
- 普通用户只能操作自己的数据
- 管理员可以操作所有数据
- 社团管理层可以操作所在社团的数据

### 3. 角色强制
- 普通用户加入社团时，角色强制为 MEMBER
- 防止普通用户提升自己的权限

---

## 🚀 部署步骤

### 1. 重启后端服务
修改了路由配置后，需要重启后端服务才能生效：

```bash
cd back
# 如果服务正在运行，先停止（Ctrl+C）
npm run dev
```

### 2. 验证修复
使用以下账号测试：

**测试招新功能**：
```
账号：admin / superadmin
密码：123456
操作：社团招新 → 发布招新
预期：成功
```

**测试加入社团**：
```
账号：user
密码：123456
操作：我的社团 → 加入社团
预期：成功
```

---

## ✅ 修复完成清单

- [x] 招新创建权限修复
- [x] 招新管理权限修复
- [x] 用户加入社团权限修复
- [x] 申请管理权限修复
- [x] 控制器层安全检查
- [x] 角色强制逻辑
- [x] 文档更新

---

## 🔮 后续优化建议

1. **社团申请审核机制**
   - 用户申请加入社团需要审核
   - 社团管理层批准后才能加入

2. **邀请机制**
   - 社团管理层可以邀请用户加入
   - 用户接受邀请后加入社团

3. **权限日志**
   - 记录所有权限相关操作
   - 便于审计和问题排查

4. **动态权限**
   - 根据社团设置调整权限
   - 支持更灵活的权限配置

---

**修复时间**：2024  
**版本**：v2.0.1
